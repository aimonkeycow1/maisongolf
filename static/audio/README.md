# 分享視頻背景音樂

將無版權或已授權的 MP3 放入此目錄，檔名需與 `share_media.py` 中 `MUSIC_TRACKS` 一致：

| 檔案 | 顯示名稱 |
|------|----------|
| `fairway_morning.mp3` | 晨間球道 |
| `sunset_round.mp3` | 夕陽收桿 |
| `clubhouse.mp3` | 會所慶祝 |

未放置音檔時，視頻仍可合成（無配樂或僅畫面）。

建議來源：Pixabay、Mixkit 等標註可商用的運動/輕音樂。
